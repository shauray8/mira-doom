# 1B-parameter latent world model: transformer dimensions overlaid onto LatentWorldModelConfig via
# the `latent_world_model@architecture.config: 1b` package override.
hidden_dim: 2048
n_head: 16
n_kv_head: 4
n_layers: 16
time_attention_every: 4
